hola aca van las historias
<% current_user.stories.each do |story| %>
  </br><%= button_to story.title, story_path(story), method: :get %>
<%end %>

<%= button_to 'New story', new_story_url,  :method => :get %>
